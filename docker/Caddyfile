# ClanHub — Caddyfile (Development)
# In production, replace localhost with your domain

localhost {
	# Frontend
	handle /* {
		reverse_proxy frontend:3000
	}

	# Backend API
	handle /api/* {
		reverse_proxy backend:4000
	}
}
